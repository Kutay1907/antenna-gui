---
description: Push the current project state to GitHub
---

1. Check the current git status
// turbo
2. Add all changes
3. Commit the changes with a descriptive message
4. Push to the origin

```bash
git status
git add .
git commit -m "Update project: <SUMMARY OF CHANGES>"
git push
```
